// ── useSound hook ──────────────────────────────────────────
// Convenience re-export — components import from here
// instead of directly from context.

export { useSound } from "@/context/SoundContext";